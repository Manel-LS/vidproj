[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: exercises ffmpeg and takes minutes
filterwarnings =
    ignore::DeprecationWarning:jose.*
    ignore::DeprecationWarning:pytest_asyncio.*
asyncio_default_fixture_loop_scope = function
