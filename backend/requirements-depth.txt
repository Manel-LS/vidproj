# Optional: depth-based 2.5D parallax.
#
# Kept out of requirements.txt because it is ~200 MB installed for a feature that
# also needs a model file, and every deployment that does not use it would pay for
# it. Without these the app runs exactly as before: scenes keep the flat camera
# moves and /capabilities says why the depth effect is unavailable.
#
#   pip install -r requirements-depth.txt
#   python scripts/fetch_depth_model.py
#   # then DEPTH_PROVIDER=onnx in .env
-r requirements.txt
onnxruntime==1.29.0
